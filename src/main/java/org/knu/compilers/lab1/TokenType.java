package org.knu.compilers.lab1;

public enum TokenType {
    KEYWORD,
    IDENTIFIER,
    OPERATOR,
    STRING_LITERAL,
    INTEGER_LITERAL,
    FLOATING_POINT_LITERAL,
    SEPARATOR,
    WHITESPACE,
    COMMENT,
    LEXICAL_ERROR,
    EOF
}
